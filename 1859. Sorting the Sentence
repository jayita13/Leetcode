https://leetcode.com/problems/sorting-the-sentence/


def sortSentence(self, s: str) -> str:
        res = [''] * len(s)
        words = s.split()
        
        for i in words:
            res[int(i[-1])-1] = i[:-1]
             
        return ' '.join(res)
